## ADDED Requirements

### Requirement: Comparison Target Selection
The system SHALL allow the user to select which product to compare against the current product.

#### Scenario: Select target
- **WHEN** in Compare Mode, user clicks on a product thumbnail from the selection list
- **THEN** system updates the right-side overlay with the selected product's style.
