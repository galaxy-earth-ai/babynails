## 1. Context & State

- [x] 1.1 Create `VtonContext` to manage `userHandImage` and `isProcessing` state
- [x] 1.2 Wrap `CartProvider` or `Layout` with `VtonProvider`

## 2. Components

- [x] 2.1 Build `VtonUploader` with drag-and-drop and camera support
- [x] 2.2 Build `ProcessingAnimation` with a scanning laser effect using Framer Motion
- [x] 2.3 Build `VtonModal` to house the entire try-on experience

## 3. Integration & Logic

- [x] 3.1 Update `ProductDetailPage` to include the "Virtual Try-On" button
- [x] 3.2 Implement logic to "swap" the result image based on the selected product (using the mock results we generated earlier)
- [x] 3.3 Add a "Before/After" toggle (Preparation for future comparison feature)

## 4. Refinement

- [x] 4.1 Ensure the transitions between "Original Hand" and "AI Result" are seamless
- [x] 4.2 Verify persistence across different product pages
