## 1. Setup & Data

- [x] 1.1 Create product data structure and sample products in `src/lib/products.ts`
- [x] 1.2 Implement `CartContext` with `localStorage` persistence in `src/lib/CartContext.tsx`
- [x] 1.3 Update the root `layout.tsx` to wrap the application with `CartProvider`

## 2. Components

- [x] 2.1 Create a reusable `ProductCard` component with "Add to Cart" button
- [x] 2.2 Create a `CartDrawer` component using Framer Motion for the sliding effect
- [x] 2.3 Update the main `Navbar` component to include the "Shop" link and an animated `CartIcon` with badge

## 3. Shop & Product Pages

- [x] 3.1 Implement the `/shop` route with a responsive product grid
- [x] 3.2 Implement the dynamic `/shop/[slug]` route for detailed product views
- [x] 3.3 Add "Empty Cart" states and "Success" feedback after adding items

## 4. Refinement & Styling

- [x] 4.1 Apply the Soft/Pastel Luxury aesthetic to all new elements
- [x] 4.2 Add micro-interactions (hover states, button scales) using Framer Motion
- [x] 4.3 Verify all scenarios from specs (adding/removing items, refreshing page)
